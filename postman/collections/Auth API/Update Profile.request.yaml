$kind: http-request
name: Update Profile
method: PUT
url: "{{baseUrl}}/api/auth/profile"
order: 4000
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "username": "alex1",
      "email": "alex20@timetoprogram.com"
    }
